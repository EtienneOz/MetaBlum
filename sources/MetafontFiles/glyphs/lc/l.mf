% box dimension definition %
beginadjchar("l",1,cap#,0);

% sidebearings %
numeric l,r;
normal_adjust_fit(space*0.18u#,space*0.18u#);

% construction %
x1=x2=.5w;
y1=0;
y2=h;

% draw %
draw z1--z2;

% composite glyphs %
charanchortops_[charcode]=(0.5w,h+1/6cap);

penlabels(range 0 thru 30);

endchar;